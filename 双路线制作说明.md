# 双路线制作说明

更新：2026-09-12。已接入本地工作流，并以黄已经历史单完成两版静态内部重制。这里的“质量路线”表示追加制作投入，不表示已经获得客户认可或达到竞赛级效果。

## 怎么选择

默认内部规则：计费单价低于10元/页走省Token路线，10元/页及以上走质量路线。这是根据现有价格区间设置的可调整建议，不改你对客户的报价。阈值在 `config/routing_policy.json`。

- 1—2元/页：以现成可编辑版式和完整资料为前提，优先只排版。不要默认为这个价格包含文案策划、搜集事实或无限修改。
- 4—6元/页：默认省Token路线，使用更合适的已有版式；需要大幅策划、复杂图表或专属插画时标范围冲突，由你判断。
- 6—10元之间：当前配置仍为省Token路线；可根据实际要求人工改走质量路线并记录理由。
- 10元及以上：默认质量路线。报价高并不能自动授权购买素材或外部服务。
- 价格未知：只给暂定建议。总价除以**计费页数**，不除以包含赠页的实际成稿页数。
- 低价复杂单：不静默降低完整性，不自动涨价。你决定缩小范围、重新议价或承接。

当前Codex负责调用 `workflow.py route 订单号 路由需求.json`，你不用写JSON。路线及依据进入订单和任务包；新客户事实到来会标记路线需要复核。

## 每单执行

| 环节 | 省Token路线 | 质量路线 | 你需要做什么 |
|---|---|---|---|
| 收资料 | 只读本单消息和附件，哈希去重 | 同左 | 明确客户授权范围和文件位置 |
| 需求核对 | 一份文案与来源表，列缺项 | 同左，额外检查叙事与图表需求 | 确认价格、交期、缺失事实 |
| 对标检索 | 文件名索引先搜，最多看3份候选 | 最多看5份候选，必要时看少量关键页 | 风格不明确时选定方向 |
| 制作 | 复用可编辑版式，不每单重写生成器 | 核心页单独布局，按需插画和图表 | 判断是否值得追加投入 |
| 改稿 | 只读反馈、受影响页和关联事实 | 同左，必要时联动调整关联页 | 决定新增需求是否超范围 |
| 检查 | 文案覆盖、原生文字、最终全页预览 | 同样底线，额外调整细节和重点页 | 最终业务内容和审美确认 |
| 交付 | 样稿/终稿分开，先本地打包 | 同左 | 实际发送、验收和收款 |
| 复盘 | 记录真实工时、修改原因、未知成本 | 同左 | 补实际花费及客户反馈 |

候选数量是当前操作预算，不是已完成整个模板库的视觉索引。教学生成器已支持共用内容输出两个版式分支；其他类型PPT仍需按内容选择或新增布局，不是任意需求一键成稿。

## 网页委派什么时候值得用

已实际使用 `delegate-to-chatgpt-pro`，网页起草了路由器及测试，本地审查后接入。适合一次交清输入、独立输出较完整结果的工程设计、代码起草、复杂审查。小修改留在本地，避免发送和读取长篇回复的额外开销。

本次网页只收到通用工程合同，没有微信原文、联系人资料或客户附件。原技能当前的通用合同偏工程，不直接宣称支持任意PPT制作。后续若委派客户内容，先明确最小材料与外发范围，不能自动把整份微信导出上传。

网页调用仍消耗网页端额度；Codex也消耗浏览操作、审查和集成所需上下文。此次未取得完整逐请求Token/金额，**不宣称节省百分比**。网页本次重试一次才成功；后续最多两轮修正，无法稳定返回时继续本地执行。

## 真正降低重复成本

1. 200多GB案例库在本地建立索引，每单只提交命中的少量记录。读取磁盘和XML不会按文件GB数直接消耗模型Token。
2. 同一文件哈希对应一份解析缓存，旧源不重复入上下文。
3. 每单只读当前任务包；完整聊天留在私有原始档案，按需检索。
4. 内容和版式分离。同一份 `content.json`驱动两条路线，不维护两套易分叉的文案。
5. 冻结客户已经确认的内容和风格，反馈映射到具体页，优先局部修改。
6. 程序做确定性检查，模型检查真正需要判断的内容与视觉。全页渲染在本地完成。

## 本单沉淀与下次验证

- 原首稿23页没有原生文字：以后资料检查先识别图片型PPT；内容可编辑作为交付底线。
- 18页可编辑补发版漏了导读：以后分别记录导读页数和正式课页数，不只记录总页数。
- 《修改.docx》的第12—16页属于正式课：插入4页导读后对应成稿16—20页，修改前先建立页码映射。
- 承诺页“至少分钟”缺数值：本次保留明确填写空格；不替客户捏造时长。
- 引文没有完整出处：保留客户材料的来源身份和待核标记，不把客户提供等同于已核实。

以上作为候选经验。下次真实订单记录是否触发、执行动作、修改次数、实际人工分钟和客户结果，跨单验证后再提升为通用规则。本轮是历史回放，不能证明真实盈利或修改率已下降。
